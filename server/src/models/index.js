import DataModel from './DataModel';
const requests = new DataModel('requests');
const contacts = new DataModel('contacts');
const users = new DataModel('users');

export default {
  requests,
  contacts,
  users,
};
